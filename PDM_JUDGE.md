# PdMジャッジ｜権限設定モック

対象: 会社画面の権限設定UI（権限一覧／権限作成・編集（統合）／指標カタログ／役割サマリ／ユーザー割当）
判定日: 2026-06-29 ／ 対象コミット: 96608e0 以降

## 総評

**判定：Go（プロト合格・本実装は永続化/監査の要件確定が前提）**

「ロール × 機能カテゴリ」を2軸（一覧・分析／設定）に分け、操作は閲覧を内包する順序権限（なし⊂閲覧⊂操作⊂全て）で表現する設計が安定。**新規作成と既存編集を1つの3ステップ統合画面に集約**（タイトル出し分け）し、①ベース選択（ハードプルダウン・デモ別出し分け）→②画面/列ごとの可否を直接編集→③基本設定、という思考順のフローにまとまった。デモ切替（導入／運用）で「母体を作って後で分ける」ライフサイクルも体現。プロトタイプとしての完成度は高く、顧客提示・要件すり合わせに十分使える。

## 良い点（Keep）

- **2タブ分離**（一覧画面権限／設定画面権限）で「見る権限」と「操作権限」の混在を解消。
- **操作＝閲覧可の併記**・選択UIの包含点灯（操作選択時に閲覧も点灯／全ては単独）で階層を可視化。
- **デモ切替（導入／運用）** で導入→運用のライフサイクルと「母体→分割（本部管理→経理/人事）」を体感的に提示。新規導入は推奨5権限が登録済み。
- **作成/編集の統合3ステップ画面**：ベース（テンプレ/既存コピー/プレーン）→ 画面＋列ごとに可否を直接編集（大元スイッチは配下列に連動）→ 名前・区分・スコープ。新規/編集をタイトルで出し分け。
- **段階的な粒度**：概要（許可数）→ 画面ごと → 列（機能・項目）ごと、をドリルダウンで編集。深い詳細（指標カタログ等）は別画面で併存。
- **行/列の絞り込みを階層化**：タブ上の**表示ロール選択**（会社/店舗の2大別＋内訳・両タブ共通スコープ）で行、列プルダウンで列を制御。行→領域（タブ）→列の階層が明快。
- **オーナー（全権）を一覧から除外**（全権固定で編集対象外）。設定対象（会社/店舗）を主役化し、オーナー専用機能（カタログ管理/付与先指定）はヘッダーの「オーナー操作」群に集約。
- **配信の恒久キャッシュ対策**：ビルド版数自動注入＋ネットワーク優先の自動更新＋版数可視表示。デモ提示時に「古い画面が出る」事故を防止。
- **セルクリック→設定詳細**遷移、**一括設定**、**列の自動記憶＋既定で主要列表示** で運用負荷に対処（名前付き保存ビューは過剰として廃止し簡素化）。
- 権限種別の整理（base/view/edit/admin/action）と、表示専用画面と操作画面のカテゴリ分離（従業員（閲覧）／従業員管理）。

## 課題・リスク（Problem）

| # | 重要度 | 内容 | 対応案 |
|---|---|---|---|
| 1 | 高 | 永続化が **localStorage（ブラウザ依存）**。端末・ユーザー間で共有されず監査もなし | 本実装はDB保存・全社共有・監査ログ |
| 2 | 高 | **変更の保存単位・差分・ロールバック**が曖昧（参照で即時反映、保存はトーストのみ） | 差分プレビュー・確定/取消・承認フロー・履歴 |
| 3 | 中 | **「一部（混在）」の欠落機能**が詳細を開かないと不明 | 一部セルのツールチップ/ポップで欠落要約 |
| 4 | 中 | **テンプレ自体の管理**（追加・改名・自社標準テンプレの保存）が未対応＝固定5種のみ | テンプレのCRUD（自社雛形の登録） |
| 5 | 中 | **スコープ（自店舗/掌握店舗群/全社）**は表示中心で編集UIが弱い | ロール作成時のスコープ設定フロー強化 |
| 6 | 中 | 兼務ユーザーの**実効権限の機能粒度可視化**が弱い（合算は範囲のみ） | 実効権限ビュー（合算結果を画面粒度で） |
| 7 | 中 | **編集系統が2つ併存**（統合3ステップ画面＝画面/列粒度／別画面 view-edit＝指標カタログ等の深い粒度）。役割の線引きが曖昧 | 統合画面に「詳細編集へ」導線を明示 or 段階統合 |
| 8 | 低 | カテゴリ多数時の**列検索/グルーピング**が無い（横スクロール＋自動記憶で緩和） | 列検索・カテゴリ検索 |

## 想定ユースケースの充足

- 導入時に推奨ロールの土台を素早く用意 … ◎（権限テンプレート＝雛形＋確認＋プレビュー）
- 運用時に1ロールを追加（テンプレ/複製） … ◎（プレビュー付き作成）
- 既存ロールの権限を俯瞰・比較・微調整 … ◯（マトリクス＋区分列＋セルクリック編集）
- 職務分掌（経理/人事）の本部専用機能の出し分け … ◯（dept所管で自動／母体から派生）
- 兼務ユーザーの実効権限把握 … △（範囲は表示、機能粒度の合算可視化は弱い）
- 監査・変更履歴・承認 … ✕（モック範囲外。本実装の必須要件）

## 次アクション（優先順）

1. **永続化・全社共有・監査/履歴**の要件確定（課題1,2）— 最優先。
2. **テンプレ管理（自社標準テンプレのCRUD）**（課題4）— 「母体→派生」を本運用に乗せる鍵。
3. 「一部」の**欠落機能サマリ**（課題3）— 低コスト・高価値。
4. **スコープ設定フロー**の強化（課題5）。
5. 兼務の**実効権限ビュー**（課題6）。

## ジャッジ要約

- 設計・UX・ユースケース網羅は **合格**。導入〜運用のストーリーを1モックで提示でき、商談・要件すり合わせに有効。
- 本実装GOの条件は **永続化/共有/監査・変更管理** と **テンプレ管理・スコープ** の要件確定。
- 顧客依存で割れやすい論点（予算/勤怠連携など他機能との権限境界）は別途すり合わせ。

---
※モックはセッション内/ブラウザ保持・架空データ（麺屋ミライ）。本判定はUI/UX・情報設計の観点であり、実装/セキュリティ監査は別途。

- **会社指標を全ロール開放＋既定OFF/オプトインに緩和**（2026-07-02）：P「全権(会社ロール)は両タブ、店舗ロールでも会社指標の一部を見せたいニーズあり。ただし会社営業利益等は初期は見えない・変更可」→ C実装=(1)店舗/会社タブを全ロールで表示(旧:会社ロールのみ両タブ)(2)会社指標を店舗ロールにも付与可＝ハードブロック撤回(旧:全社集計は店舗ロールに出さない)(3)行データのスコープ遮断(for_user)は独立にハード強制のまま＝付与≠他店明細解放。仕様書一式の「越境防御=ハードブロック」記述を整合更新。Keep（顧客ニーズ優先・情報漏洩は行スコープ層で担保）。

- **会社指標を4→25に拡充**（2026-07-02）：P「会社指標はこれで全部か？もっとあるはず」→ C確認=代表4指標のみで不足。店舗系統(売上/仕入・原価/人件費/固定費/利益)の全社集計版を網羅＋店舗横断(ランキング/構成比)系統を追加し25指標に。Keep（会社俯瞰の指標母集合として妥当）。

- **会社指標の機密バッジ＝開示推奨度の2段階（案B）**（2026-07-02）：P「会社指標に"店長まで"バッジがあるが正しいか／店長が見るべき会社全体の指標という意味か」→ AskUserQuestion=案B採用。バッジは「店舗ロールへの開示推奨度」＝🔸店長まで(ベンチマーク系:全社の客数/客単価/前年比/来店組数/全店平均客単価/廃棄ロス/総労働時間/人時売上高)は店長・エリア長 既定ON・従業員 既定OFF、🔒経営機密(利益/原価/人件費/家賃/販促費/ランキング/構成比＝会社全体の営業利益等)は全店舗ロール 既定OFF。会社ロール全ON。管理者が変更可。既定可視ロジックを sens 依存に変更(旧:全社指標は店舗ロール一律OFF)、販促費(全社)を mgr→exec 修正。Keep（バッジが開放判断の目印として機能・現場には出さない意味と一致）。

- **指標条件設定②の上部「＋条件を追加」をモーダル化**（2026-07-02）：P「上部の条件追加を押すと設定項目が下段に出て分かりにくい。上はモーダルがよいか、切り分けの意見を」→ C意見=賛成(同一動作のボタンで結果が画面外に出るのが混乱源)＋原則(両ボタンとも同一の condRules に書込＝概念は1本)。実装=上部はモーダル入力→末尾追加＋該当行スクロール&ハイライト＋トースト、当初は最下段=インライン追加。→同日P「上下2つの＋条件を追加、UI上大丈夫か」→ AskUserQuestion=案A採用で**上下ボタンとも共通モーダルに統一**（同ラベル＝同挙動）。既存ルールの編集/削除は下段インライン行のまま＝追加=モーダル・編集=インラインで一貫。旧 addCondRule は0参照で撤去。Keep。

- **権限の適用方向**（2026-06-29）：P「設定した権限をユーザにどう適用？従業員一覧/管理者一覧から選択して適用か」→ C回答=ロール経由(権限→ロール→ユーザー)・対象は経営管理ログインの**管理者一覧(master連動)**で全従業員ではない・複数ロール和集合・店舗ロールは店舗束縛。P採用=管理者一覧に適用の方向でOK。Keep。

- **権限編集エディタの統合**（2026-06-29）：P「セルと権限編集ボタンで遷移先が違う／アコーディオンとウィザードで機能差・重複は？」→ C調査=アコーディオンは操作詳細/指標まで最深、ウィザードはテンプレ/基本設定を持つが浅い＝中間層重複。P採用=ウィザードに一本化し最深部を移植・セルはズーム展開。Keep（俯瞰→ドリルダウンを1画面に集約・二重保守解消）。
- **詳細UIのボタン様式統一**（2026-06-29）：P「操作詳細・指標がチェックボックス（アコーディオン引継ぎ）。ウィザード様式のボタンに。閲覧可能指標はトグルで開く」→ C採用=操作詳細〔許可〕〔なし〕／指標〔表示〕〔非表示〕／読取chip、閲覧可能指標はトグル折りたたみ。Keep。
- **catStatus根本修正**（2026-06-29）：P「一覧=閲覧可なのに詳細=操作可。店長も操作可と表記すべき。徹底確認を」→ C特定=操作(2)をmaxV(3)でないため閲覧誤判定＋lockをなし誤判定。修正し118件 不一致0。Keep（表示=実態の整合は信頼性の要）。
- **ダッシュボード=ランディングの閲覧固定＋指標自動反映**（2026-06-29）：P「ダッシュボードは全権限で閲覧可・閲覧可否設定不要。カスタマイズは操作なので操作列。管理者が閲覧可にした指標は従業員ダッシュボードに自動反映」→ C採用=閲覧固定(ランディング)＋設定タブに「ダッシュボード(カスタマイズ)」列＋閲覧可能指標は指標一覧の自動反映(読取)。Keep。

- **操作詳細のCRUD連動**（2026-06-29）：P「登録ができれば編集も削除もできるべき、操作⊃閲覧と同じく連動すべき」→ 連動の向きを確認(AskUserQuestion)＝P選択「削除⊃編集⊃登録（標準CRUD）」。C実装=上位許可→下位許可/下位なし→上位なし。Keep（不整合状態の防止）。
- **表示=実態の整合（screenSeg群対応）**（2026-06-29）：P「一覧=閲覧可なのに権限編集で閲覧が点灯しない。他も全部確認して」→ C特定=screenSegがview群の'op'を2にマップし閲覧(1)に当たらず未点灯。群対応で修正、全118件 不一致0。Keep。
- **詳細のトグル化・閲覧画面の整理**（2026-06-29）：P「閲覧画面なのに操作トグルが出る。操作詳細はトグルで閉じて開いた時に出す方がいい。遷移元で画面を出し分けるべきか」→ C意見=別画面化は統合の利点を失う→非推奨。代わりに操作詳細/閲覧可能指標をトグル折りたたみ＝閲覧画面は既定クリーン＋フォーカス(セルはズーム)。P採用=その方向。Keep。
- **旧アコーディオン撤去**（2026-06-29）：P「未使用のview-editをコードから撤去」→ C実施=HTML＋関数群約190行削除し統合ウィザードに一本化。巻き込まれたgotoCatEdit(セル遷移)はvm検証で検知し復元。全パス動作確認。Keep。

- **操作詳細の連動の向き是正**（2026-06-29）：当初AskUserで「削除⊃編集⊃登録」が選択されたが、Pが再度明示「登録ができれば編集や削除も連動して操作可能として選択される」→ C是正=登録(親)ON→編集・削除も連動ON/登録OFF→全OFF、編集・削除は個別調整可。明示指示を正とする。Keep。
- **操作詳細/指標ボタンの点灯バグ修正**（2026-06-29）：P「操作詳細・閲覧可能指標のボタンが機能していない」→ C特定=ハイライトCSSがdata-v限定で、許可/なし・表示/非表示(data-v無し)に色が付かず＝状態は変わるが見た目不変。data-on属性＋CSSで解消。Keep。

- **キャッシュ機構の根本修正**（2026-06-29）：P「ボタンが機能しない／反映されない」が繰り返し → C調査=コードは正しいが、Pagesがlegacy(ブランチ直配信)でビルド版数SHAが未注入＝自動更新が常に無効＝古いキャッシュ滞留が真因。build_type=workflowへ切替し実SHA注入を確認。Keep（恒久対策の前提条件。legacyに戻さない）。教訓：「キャッシュ」で片付けず配信方式まで疑う。

- **操作詳細連動と4状態の整合（案C）**（2026-06-29）：C指摘=「登録(親)→削除」連動が4状態(削除=全ての境界)と矛盾＝操作なのに削除が入る不整合。3案提示しP選択=案C。実装=登録↔編集連動／削除は確認付きopt-in＋管理付与(=全て相当)。Keep（意図(管理できる)と安全(誤削除付与防止)＋4状態整合を両立）。

- **並び順の統一・機能序列**（2026-06-29）：P「店舗ロールは管理者→従業員／設定画面の並びは一覧と同じ重要度順／カテゴリ内は大元設定→中設定→一覧→分析(閲覧は逆順)」→ C実装=店舗ロール権限順・orderedCats(重要度順を一覧/権限編集/ツールバー共通)・featTypeRank(カテゴリ内序列)。P確認「月次予算設定→単月→売上構成比→曜日別バイアスの順は良い」。Keep。
- **費用一覧の統合**（2026-06-29）：P「費用一覧という画面は無く費用実績入力が兼ねる」→ C撤去し「費用実績入力・一覧」に統合。実UI準拠。Keep。

- **本番設計・ロードマップ・エンジニア共有一式の整備**（2026-06-29）：P「本番enforcement設計→feature_key辞書→実装優先順位→P0詳細→エンジニア共有準備」を順次依頼。C作成=ENFORCEMENT/ROADMAP/P0_FOUNDATION/KICKOFF/feature_keys.json。原則=安全>価値>速度・段階導入(シャドー→部分→全面)・本番非破壊。Keep（UI仕様から実装着手まで連続して渡せる状態に）。
- **仕様書⇔モックの整合監査**（2026-06-29）：P「仕様書とモックは整合あるか」→ C13項目を機械監査=12一致・1不整合(費用一覧撤去で66→65なのにENFORCEMENT辞書が古い)を検出し辞書再生成で修正。Keep（機能増減時は辞書/JSON再生成を規律化）。

- **区分とスコープの独立・スコープの明示**（2026-06-29）：P「従業員が複数店舗所属／従業員に会社ロール／データ範囲=会社 もあり得る」→ C採用=区分(画面)とデータ範囲(スコープ)は別軸で組み合わせ自由・実効スコープは割当(user_roles)で束縛・ベース差異は助言警告(ブロックせず)・越境防御は指標の機密×実効スコープでゲート(区分に依存しない)。仕様書の硬いスコープ規則を緩和。権限編集にロール単位の範囲バナー＋range機能に範囲バッジ。Keep。

- **操作⊃閲覧の点灯復活＋整合確認**（2026-06-29）：P「操作可能なら閲覧も自動選択されるはず／一覧では閲覧可と表記」→ C修正=アコーディオン撤去時の移植漏れで、ウィザードのfeatSeg/screenSegが操作(2)選択時に閲覧(1)を点灯していなかったのを是正(全て(3)は全てのみ)。一覧マトリクスは既に「操作+閲覧可」表記で整合。README §選択UIの点灯は既に仕様記載済み＝コードが仕様に追従。仕様書⇔モック整合監査=全17項目OK・不整合0。Keep。

- **店舗紐付けをユーザー割当に一本化**（2026-06-30）：P「権限編集とユーザー割当の2箇所で店舗紐付け？→寄せて。自店舗と複数店舗を紐付けできるように」→ C実装=店舗の実紐付けはユーザー割当(割当編集モーダルに対象店舗選択)に一本化、権限編集③はスコープ型のみ(具体店舗チェック撤去)。理由=店舗は人に紐づく事実・user_roles.store_idsが真実・ロール増殖と二重管理の回避。有効範囲列にユーザー実店舗を表示。Keep。

- **エリア長を権限で差別化（店長は複数店舗可）**（2026-06-30）：P「店長でも複数店舗掌握あり得る。ならエリア長は必要か？」→ AskUserQuestion=「権限で差別化して残す」。C実装=PRESET.area新設(編集系=全て/削除可)で店長(操作)の上位に＝17機能で権限差。スコープ(店舗数)は人ごと自由で店長も複数店舗可(単一店舗制限案は撤回)。割当UIはロールを店舗/会社でグループ化＋店舗未設定警告(保存不可)。Keep。

- **エリア長＝店長の拡張版・顧客組織依存**（2026-06-30）：P「エリア長は店長権限の拡張版。内容は顧客の組織構造による」→ C確認=PRESET.areaは店長のsuperset(店長⊂エリア長)で構造充足。拡張内容は固定せず権限編集で店長ベースに上乗せして各社定義(既定は雛形)。仕様書に明記。Keep。

- **権限編集からデータ範囲を撤去（区分は残す）**（2026-07-01）：P「基本設定の区分は要るがデータ範囲は割当でできそう。意見を」→ C同意=データ範囲(スコープ)は割当で確定するため権限編集から撤去し「権限名＋区分」に整理。区分は会社専用機能のゲート＋割当の店舗ピッカー出し分けの軸として残す。二重管理を完全排除(スコープの真実は割当のみ)。Keep。

- **役割サマリに割当ユーザー列＋割当へ導線**（2026-07-01）：P「ロール割当をユーザー割当でなく権限設定側でもできた方がいいか？」→ C=主たる割当はユーザー割当(店舗紐付けが人単位のため)に残しつつ、権限設定側の役割サマリに「割当ユーザーN名＋割当へ→」の逆引き＋導線を追加。フル割当UIの複製は避け二重管理を防止。Keep。

- **権限編集に割当プレビュー＋未設定ステータス（案B）**（2026-07-01）：P「店舗紐付けを割当へ移したのは正解か／遷移ボタンかプレビュー表示か」→ 移動は正解(店舗は人単位)だが失った可視性を復活すべく案B採用。権限編集③に「割当ユーザー×対象店舗」プレビュー(実店舗＋未設定⚠)を表示、役割サマリに未設定警告。閲覧は権限設定・編集は割当の分担。遷移ボタン(案A)は併存。Keep。

- **複数ロール割当は現状維持（案A）**（2026-07-01）：P「割当画面で複数ロールが選択可能なのは大丈夫か」→ C確認=意図的（兼務対応・権限の和集合）。越境防御は指標の機密×実効スコープでゲートするため合算しても漏洩なし。ガードレール（冗長組合せ警告・1人1ロール制限）は入れず現状維持を選択。README §ユーザー割当に記載済み。Keep。

- **割当は「既存権限の適用」モデルで確定（案A）**（2026-07-01）：P「割当画面のチェックは既存ロールを人に適用する形／新規作成は一覧に無い新ロールを作るボタン、という理解か」→ C確認=そのとおり(RBAC)。権限はロールとして一度定義し共有、割当は既存ロールの適用(合算)。人ごとの権限作り込みはしない(増殖防止)。チェック→新規作成扱いにはしない。Keep(案A)。

- **権限編集⇄割当の相互遷移・戻り導線を整備**（2026-07-01）：P一連の指示→ C実装=①データ範囲を権限編集から撤去し「権限名＋区分」に(スコープは割当が唯一の真実)②権限編集③に割当プレビュー(掌握店舗の集約・未設定警告)＋「ユーザー割当で紐付け→」CTA③保存して移動の遷移先は既存ロール=保持者の割当編集画面/新規ロール=役割サマリ(ハイライト)、権限名未入力は不可④役割サマリ「割当へ」はそのロールの割当ユーザーに絞り込み(管理者一覧の直接アクセスは全件のまま)⑤権限編集の戻るは来た元＋「権限一覧へ」の2ボタン(役割サマリ経由/割当編集画面経由)⑥割当画面からの新規作成は①ベース=プレーンテンプレート既定。閲覧は権限設定・編集は割当の責務分離を貫徹。Keep。
