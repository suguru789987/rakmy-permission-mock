# 権限設定 ステージング検証 実施手順

2026-08-10 ／ 使うもの：検証プラン（`20260804_権限設定_02_検証プラン.xlsx`）・検証用データセット（`20260806_権限設定_03_検証用データセット.xlsx`）

---

## 事前に用意するもの

| # | 用意するもの | なぜ必要か |
|---|---|---|
| 1 | ステージングのURL | S-01 で控える。プラン S-01 の操作欄が空欄になっている |
| 2 | **別の全権アカウント** | T-17 で自分を締め出す恐れがある。復旧用 |
| 3 | 検証用のメールアドレス8個 | `+pm01`〜`+pm08` で区別（dataset/07 のとおり） |
| 4 | スクショの保存先フォルダ | 検証IDの名前で保存する（T-11.png など） |

**本番の rakmy／rakmy_server には触れないこと。** 顧客名・従業員名はダミー（麺屋ミライ／検証◯◯）を使う。

---

## 手順

### STEP 0　移植前スクショを撮る（S-06）★最優先

プリセットを適用する前に、master／manager／shop_manager でログインして**全メニューを3枚**撮る。

**リリース前にしか撮れない。** これが無いと T-21（現行3権限と同じ動作か）の比較対象が失われ、AC-01〜03 が判定不能になる。

保存名：`S-06_master.png` `S-06_manager.png` `S-06_shop_manager.png`

---

### STEP 1　環境を確認する（S-01）

1. ステージングURLを開き、権限設定の画面が出ることを確認する
2. 仕様モック https://suguru789987.github.io/rakmy-permission-mock/ も開き、build版数が最新か見る
3. エンジニアに次を確認する
   - 権限設定画面はメニューのどこから開くか（ヘルプに「左メニューの設定＞権限設定」と書いている）
   - ダッシュボードは権限設定で「なし」にできないか（閲覧固定か）
   - 画面カテゴリを新しく追加したとき、既存の権限の既定値はどうなるか

スクショ：ステージングの権限一覧と仕様モックを並べた状態

---

### STEP 2　権限テンプレートを見る（T-04）★権限を作る前に

**この画面は「権限を1件も作っていない初回」にしか表示されない。** 順番を間違えると「画面が出ない＝NG」と誤判定する。

1. 権限テンプレート画面で、選択途中に「一時保存」→ 別の画面へ移動 → 戻る（選択が復元されるか）
2. 「下書きを破棄」→ 既定に戻るか
3. 「適用」→ 権限一覧に4権限が登録されるか

スクショ：4権限の選択欄と「適用」「一時保存」「下書きを破棄」が写った全体

---

### STEP 3　データを入れる（S-02・S-03）

データセットの「使い方」シートの投入手順に従う。

1. 店舗3件（渋谷・新宿・池袋）
2. 売上・仕入・人件費・費用・予算を**検証実施月の1日分**として登録
   - **金額の比率 1：2：4 を崩さない**（100万／200万／400万）。崩すと合計からの逆算ができなくなる
3. 従業員4名（池袋三郎の給与だけ80万）
4. 検証ユーザー8名を登録

**日付は dataset が 2026-08-01 で作られている。実施月に合わせて置換する。**

---

### STEP 4　権限を7件作る（S-04）

`08_権限設定値` のとおりに作る。

| 権限 | 区分 | 選ぶ雛形 |
|---|---|---|
| 検証_経理担当（会社） | 会社 | 本部管理 |
| 検証_経理担当（店舗） | 店舗 | 店長 |
| 検証_人事担当 | 会社 | 本部管理 |
| **検証_エリア長** | 店舗 | **店長**（既定テンプレートに無い。名前を変えて作る） |
| 検証_バイトリーダー | 店舗 | 店長 |
| 検証_閲覧のみ | 会社 | 本部管理 |
| 検証_スタッフ | 店舗 | 従業員 |

**保存したあと再度開いて、設定が再現するか必ず確かめる。** ここが壊れていると以降の検証がすべて無意味になる。

---

### STEP 5　割り当てる（S-05）

`07_検証ユーザー` のとおり。店舗の権限には担当店舗も設定する。

- US-03（エリア長）＝ 渋谷店・新宿店 の2店舗
- US-08（経理・店舗）＝ 全店
- US-01・US-02・US-05（会社の権限）＝ 担当店舗の欄が出ないことを確認

---

### STEP 6　検証を実施する（T-01〜T-24）

検証プランの上から順に。1ケースごとに次を行う。

1. 「画面」を開く
2. 「前提・入力値」と「使う検証データ」が揃っているか確かめる
3. 「操作」のとおりに操作する
4. **「期待挙動」と「起きてはいけないこと」の両方**を見る
5. 「合否ライン」に照らして「実際の値」と「判定」を書く
6. 「スクショに写すもの」のとおり撮り、「スクショファイル名」で保存する
7. 「実施者」「実施日」を書く
8. NG なら「受入条件ID」「遷移フローID」「ヘルプページの該当箇所」を見て、どこに跳ね返るかを確認し「備考」に書く

---

## 実施中の注意

**T-02** — キー重複の確認で権限を作る。**確認後に必ず削除する**（残すと T-05／T-06 の権限数が合わなくなる）。

**T-17** — 自分の権限から権限設定を外す検証。**実施前に別の全権アカウントでログインできることを確認しておく。** 警告が出ずに保存できてしまった場合は、そのアカウントで元に戻してから次へ進む。

**T-18** — US-06 の割当を外したあと **S-05 の状態に戻す**。

**T-15** — US-07 は付け替え専用なので元に戻さなくてよい。以降 US-07 は経理（会社）として扱う。

**T-23** — 会社情報を書き換えるので、**確認後に元の値へ戻す**。

---

## 判定の書き方

| 判定 | どういうとき |
|---|---|
| OK | 合否ラインを満たした |
| NG | 満たさなかった。備考に何が違ったかを書く |
| 検証不能 | データを投入できない等で確かめられなかった |

**「問題なし」と書かない。** とくにデータが入っていない場合、「塞げている」のか「データが無い」のか区別できない。T-11・T-23 は必ず実測値を書く。

---

## 終わったあと

1. 検証プランの判定を集計し、NG の件数を出す
2. 受入条件表（`20260804_権限設定_01_受入条件表.xlsx`）の「合否」を埋める
   - 検証プランの結果を見ながら、条件ごとに OK／NG を入れる
   - マイルストーンごとの到達率が自動計算される
3. NG があれば、その条件の「ヘルプページの該当箇所」を見て、顧客向けの説明も直す必要があるかを判断する
