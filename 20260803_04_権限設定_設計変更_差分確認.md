# 権限設定 設計変更 差分確認（2026-08-03）

ご提示の設計に対して、**現行の仕様モックがどこまで加味できているか**を項目ごとに突き合わせた結果。

判定：**✅対応済み** ／ **△部分対応**（考え方は入っているが形が違う） ／ **❌未対応** ／ **⚠要判断**（現行の設計原則と衝突するため決めが必要）

---

## 結論（先に3行）

- **26項目中、対応済み11・部分対応7・未対応5・要判断3。**
- 大枠（両画面からの相互紐付け、テンプレートからの作成、割当編集での権限紐付け）は**すでに入っています**。
- ただし **3点が現行の設計原則と正面から衝突** します。ここを決めないと実装に進めません（後述の「要判断3点」）。

---

## ⚠ 要判断3点（最優先）

### 判断① 1人1ロールにするか、兼務（複数ロール）を残すか

**ご提示：** 割当ユーザー設定編集の列仕様が「権限名（**1人1ロール**）」。

**現行仕様：** 1人に**複数ロールを付与でき、権限は合算**（広い方が有効）。ユーザー割当の列も「割当ロール（複数可＝合算）」。README には「鈴木（店長＝新宿店 ＋ 経理＝全社）→ 新宿店の運営と全社の経理を同時に行える」という例で明記しています。

**衝突の中身：** 1人1ロールにすると「店長 兼 本部経理」が表現できません。表現するには**「店長兼経理」という専用ロールを新設**することになり、兼務の組み合わせの数だけロールが増えます。店舗業では兼務が常態なので、ロールが人数分に近づいていきます。

**判断が必要な点：**
- (A) 1人1ロールにする → 画面と列はシンプルになるが、兼務は専用ロールを都度作る運用になる
- (B) 複数ロール（現行）を維持 → 兼務は自然に表現できるが、「この人は結局何が見えるのか」が合算結果でしか分からない

**参考：** ご提示の3の列仕様「ユーザー一覧：区分、権限名、ユーザー名」は1人1ロール前提の並びです。複数ロールを残す場合、1ユーザーが複数行に出る形になります。

---

### 判断② ユーザー行からの権限編集で「権限名の変更を強制」するか

**ご提示：** 「権限名を変更しないと全体上書きされるので、ユーザー行からの権限編集画面へ遷移した場合は、権限名を変更させる強制操作を付加する」

**現行仕様：** 権限（ロール）を編集したら、**その権限を持つ全員に反映される**のが正しい挙動。割当画面の各ロール行にある「権限編集 →」も同じ動作で、README に「そのロール自体の権限を修正（持つ全員に反映）」と明記しています。

**ご懸念は正当です。** 「田中さんだけ調整したい」と思ってユーザー行から編集すると、同じ権限を持つ全店長に波及します。事故になります。

**ただし、権限名の変更を強制すると別の問題が起きます。** 名前を変えて保存＝**その人専用の新しい権限が1つ増える**ということです。これを繰り返すと「店長」「店長2」「店長（田中）」のように権限が人数分に増殖し、**原則①（権限はロール経由で与える＝定義は少数を共有する）が崩れます**。これは現行3権限の次に問題になった、権限管理が破綻する典型パターンです。

**選択肢：**
- (A) **ご提示どおり**：ユーザー行から編集したら必ず別名で保存＝そのユーザー専用の権限を作る
- (B) **警告のみ**：「この権限は他に○名が持っています。変更は全員に反映されます」と保存前に明示し、続行するか複製するかを選ばせる
- (C) **ユーザー行からは編集させない**：権限一覧からのみ編集可とし、ユーザー行には「この人が持つ権限を見る」だけを置く

**推奨は (B) です。** 事故は防げて、権限の増殖も起きません。「複製して自分専用にする」を選べば結果として (A) と同じことができます。

---

### 判断③ 権限一覧TOPをマトリクスからシンプル一覧に変えるか

**ご提示：** 権限名一覧の列仕様は「区分、権限名、操作（**権限画面名一覧ボタン**、権限編集ボタン）」。

**現行仕様：** 権限一覧は**権限 × 画面カテゴリのマトリクス**（横に27画面分の列、セルに 閲覧／操作／— を表示）。列の表示選択や折りたたみ、セルクリックで該当箇所へズームする機能が乗っています。

**衝突の中身：** ご提示の形は「権限名が並ぶだけの縦一覧」で、**どの権限が何を見られるかは一覧では分からず、「権限画面名一覧ボタン」を押して確認する**形になります。マトリクスの「権限同士を横並びで見比べられる」利点は失われますが、画面はシンプルになり、権限数が増えても崩れません。

**判断が必要な点：**
- (A) シンプル一覧に変更（ご提示）→ 分かりやすいが、権限間の比較には都度ボタンを押す
- (B) マトリクス維持 → 比較しやすいが横に長い
- (C) **両立**：既定はシンプル一覧、「比較表示」に切り替えるとマトリクス

「権限画面名一覧ボタン」が何を出すかも確認させてください（その権限で見られる画面名のリスト＝読み取り専用、という理解で合っていますか）。

---

## 詳細の突き合わせ

### 全体（紐付けの考え方）

| # | ご提示 | 現行モック | 判定 |
|---|---|---|---|
| G1 | 権限設定と割当先ユーザー設定を紐付ける | 権限 → ロール → ユーザー の順で適用。実装済み | ✅ |
| G2 | 権限設定 → ユーザー割当 の遷移 | 権限編集の最下部に「🔗 ユーザー割当で紐付け →」あり | ✅ |
| G3 | ユーザー割当 → 権限設定 の遷移 | 割当編集の各ロール行に「権限編集 →」、下部に「＋権限を新規作成 →」あり | ✅ |
| G4 | 既登録の場合は各該当行でコメント明示 | 一括割当で既存メンバーと店舗をプリセット、店舗ロール重複に⚠、割当編集に「現在のロール」チップ | △ 近い仕組みはあるが「既に登録済みです」という明示コメントは無い |
| G5 | 紐付けが権限一覧TOPで一覧表示（タブ切替で権限一覧／ユーザー一覧） | TOPのタブは「一覧画面権限／設定画面権限」で**別の意味**。ユーザー一覧タブは無い | ❌ 新規要件 |

---

### 1. 権限設定画面

| # | ご提示 | 現行モック | 判定 |
|---|---|---|---|
| 1-1 | キーをテンプレートプルダウンに変更 | **すでにテンプレートプルダウン**（①ベース）。候補＝全権オーナー相当／本部管理／エリア長／店長／従業員／まっさら | ✅ モックは対応済み。**ステージング実装が未対応**（変更依頼 `20260727_02` で依頼中） |
| 1-2 | テンプレに「新規」を追加、選択時は全オフ | 「まっさら（なし）」＝全機能なしから開始。名称が違うだけ | ✅ 名称を「新規作成」に揃えるかは要確認 |
| 1-3 | 既存顧客ごとの権限分を転写含む | 「既存権限をコピー」がベース候補にある（運用中の権限を複製） | ✅ |
| 1-4 | ロール名を権限名に変更 | モックは元から「権限名」 | ✅ モック対応済み。**ステージングが「ロール名」**（依頼中） |
| 1-5 | 権限チェックボックス一覧を種別の下に表示 | 種別（区分）の下に画面ごとの設定が並ぶ配置は同じ。ただし**チェックボックスではなく3状態セグメント**（なし／閲覧／操作） | △ 位置は一致、様式が違う。※下記コメント参照 |
| 1-6 | テンプレプルダウン選択で権限一覧を表示 | ベースを選ぶと下の内容がそのテンプレの値で埋まる | ✅ |
| 1-7 | ユーザー割当ボタン（権限一覧の左下） | 「🔗 ユーザー割当で紐付け →」は最下部のコミット領域 | △ 機能は同じ、配置が違う |
| 1-8 | 設定権限保存ボタン（画面右下） | 最下部のコミット領域に保存 | △ 機能は同じ、配置が違う |
| 1-9 | 割当画面で従業員行のチェックで紐付け、保存で確定 | 一括割当画面で各人に〔含める〕チェック＋〔対象店舗〕、一括保存 | ✅ |

**1-5 について：** 現行が3状態セグメントなのは、権限が「なし／閲覧／操作」の**3段階**だからです。チェックボックス（オン・オフの2値）では「閲覧はできるが操作はできない」が表現できません。ただし**今回のステージング実装は閲覧制限のみ＝2値**なので、現時点ではチェックボックスで足ります。操作制限を入れる段階で3状態が必要になります。**今チェックボックスにして、次段階で作り直すか**を決めさせてください。

---

### 2. 割当先ユーザー設定画面

| # | ご提示 | 現行モック | 判定 |
|---|---|---|---|
| 2-1 | 従業員一覧で登録された従業員名が表示される | 表示対象は**経営管理にログインする管理者**（従業員台帳の master 連動アカウント）。全従業員ではない | ⚠ **要確認**（下記） |
| 2-2 | 各従業員行の割当編集ボタンで割当ロールと有効範囲を設定 | 各行に割当編集あり。ロールと対象店舗を設定 | ✅ |
| 2-3 | 割当編集で権限名ごとにチェックボックス＋権限編集ボタン＋権限新規作成ボタン | すべて実装済み | ✅ |
| 2-4 | 一覧の列：ユーザー、割当権限名、有効範囲 | 現行は ユーザー／役職（master連動）／割当ロール／有効範囲／操作 | △ **役職列が余分**。削除するか要確認 |
| 2-5 | 編集の列：権限名（1人1ロール）、対象店舗（複数可） | 権限名は複数選択可（兼務）、対象店舗は複数可 | ⚠ **判断①** |

**2-1 について：** 現行は「ログインする人だけ」を対象にしています。全従業員（アルバイト含む）を並べると、ログインしない人まで一覧に出て、誰に権限を付けるべきかが埋もれます。**全従業員を出す設計に変える場合**、ログインしない人を除外するフィルタが別途必要になります。どちらの想定かご確認ください。

---

### 3. 権限一覧TOP画面

| # | ご提示 | 現行モック | 判定 |
|---|---|---|---|
| 3-1 | 右上に権限ボタンとユーザー割当ボタン | 上部ナビに「役割サマリ」「ユーザー割当」ボタンあり。右上は「👑 オーナー操作」 | △ 機能はある、配置と名称が違う |
| 3-2 | 権限一覧タブ／権限ユーザー一覧タブで画面切替 | タブは「一覧画面権限／設定画面権限」＝別の意味 | ❌ 新規要件。**既存タブとの二重構造をどう整理するか要設計** |
| 3-3 | ユーザー行ごとに権限編集ボタン | 割当編集の中のロール行に「権限編集 →」はあるが、ユーザー一覧の行に直接は無い | △ |
| 3-4 | ユーザー行から編集時は権限名の変更を強制 | 無し | ⚠ **判断②** |
| 3-5 | 権限名一覧の列：区分、権限名、操作（権限画面名一覧、権限編集） | マトリクス表示（区分／権限名／27画面分の列／操作） | ⚠ **判断③** |
| 3-6 | ユーザー一覧の列：区分、権限名、ユーザー名、操作 | 該当画面が無い | ❌ 新規要件 |
| 3-7 | 「権限画面名一覧」ボタン | 無し（近いものとして役割サマリの許可数、指標一覧ボタンがある） | ❌ 新規要件。**何を表示するか要確認** |

**3-2 について：** 現行の「一覧画面権限／設定画面権限」タブは、27画面を性質で2群に分けて横幅を抑えるためのものです。ここに「権限一覧／ユーザー一覧」タブが加わると**タブが2階層**になります。整理案は3つ：
- (A) 上位タブ＝権限／ユーザー、下位タブ＝一覧画面／設定画面（2階層を明示）
- (B) 判断③でマトリクスをやめるなら下位タブは不要になり、タブは1階層で済む
- (C) ユーザー一覧は別画面（現行のユーザー割当）のままにし、タブにしない

**判断③でマトリクスをやめる場合、(B) が最もすっきりします。**

---

## まとめ：確認いただきたいこと

| # | 確認事項 | なぜ決める必要があるか |
|---|---|---|
| 1 | **1人1ロールか、兼務（複数ロール）か** | 画面の列構成が変わり、兼務の扱いが決まる |
| 2 | **ユーザー行からの権限編集で名前変更を強制するか** | 権限が人数分に増殖するかどうかが決まる（推奨＝警告方式） |
| 3 | **権限一覧をシンプル一覧に変えるか** | TOPの構造とタブの階層が決まる |
| 4 | 「権限画面名一覧ボタン」は何を表示するか | その権限で見られる画面名のリスト（読み取り専用）という理解で合っているか |
| 5 | 割当対象は全従業員か、ログインする管理者だけか | 一覧に並ぶ人数が変わる |
| 6 | 権限設定をチェックボックス（2値）にするか | 今は閲覧のみなので足りるが、操作制限の段階で3状態が必要になる |
| 7 | ユーザー割当一覧から「役職」列を外すか | ご提示の列仕様には無い |

上記が固まり次第、仕様モックに反映します。1〜3が決まれば残りは実装の細部として進められます。
