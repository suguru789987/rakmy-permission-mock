# 権限設定 ステージング UI 変更依頼（2026-07-27）

対象画面：**ロール編集 → 基本情報**

## 現状

| 項目 | 現在の表示 | 値の例 |
|---|---|---|
| キー | テキスト入力 | `viewer` |
| ロール名 | テキスト入力 | `閲覧テスト` |
| 種別 | プルダウン | `店舗` |

---

## 1. 「ロール名」→「権限名」に変更したい

社内でも顧客説明でも呼称は「権限」で統一しています。「ロール」は実装側の用語で顧客には伝わりません。
画面名（権限設定・権限一覧・権限編集）とも揃います。

---

## 1-2. 「表示ロール」→「表示権限」に変更したい

権限一覧のフィルタ「👤 表示ロール」も同じ理由です。ヘルプページで顧客に「権限」と説明しているのに、画面のボタンだけ「ロール」だと辿り着けません。**顧客の目に触れるラベルから「ロール」を一掃したい**という趣旨です。

## 1-3. 権限テンプレート画面の「会社ロール（権限母体）」から「ロール」を外したい

初期設定の権限テンプレート画面にある「会社ロール（権限母体）」も、顧客の目に触れるラベルです。**会社の権限（母体）** のように「ロール」を使わない表記にしたいです。1-1・1-2 と同じ趣旨で、顧客向けの画面から「ロール」を一掃したいという依頼です。

なお「店長（権限母体）」「従業員」「オーナー（全権）」はそのままで問題ありません。

## 2. 「種別」→「区分」に変更したい

仕様書上の呼称は「区分」（店舗／会社）です。あわせて統一したいです。

---

## 3. 「キー」は顧客に手入力させたくない

**先に確認したいこと：この「キー」は何を指していますか。**

**(a) システム内部の識別子（コードから参照する値）である場合**

顧客に見せず、**内部で自動生成**にしてください。顧客が英数字の一意な識別子を考える必然性がなく、重複や打ち間違いで事故ります。**権限名を入れるだけで権限が作れる**状態が理想です。

**(b) 権限範囲（データ範囲）の設定である場合**

ラベルを「権限範囲」に変えるだけでは済まず、設計の擦り合わせが必要です。仕様では**権限範囲＝ユーザー割当で対象店舗を紐付けて決める**（`user_roles` 側が唯一の真実）としており、権限定義側にも範囲を持つと二重管理になります。「権限は権限、範囲は割当」の境界を崩さない形にしたいです。

値が `viewer` で、別途「種別＝店舗」がある構成からは **(a) と読めます**。認識が合っているかご回答ください。

---

## 4. 権限テンプレート（ベース）のプルダウンが見当たらない

仕様モックでは、権限を新規作成するとき **①ベースをプルダウンで選ぶ** 作りになっています。

| ベース候補 | 内容 |
|---|---|
| 全権オーナー相当 | 全権 |
| 本部管理 | 全社の設定・一覧を操作／閲覧 |
| エリア長 | 掌握店舗の設定・一覧を操作／閲覧 |
| 店長 | 自店舗の設定・一覧を操作／閲覧 |
| 従業員 | 閲覧中心 |
| プレーン | 全て「なし」から作る |
| 既存権限をコピー | 運用中の権限を複製して差分だけ直す |

**理由**：顧客は27画面分をゼロから設定できません。近いテンプレートを選んで差分だけ直すのが実運用の形です。今回の検証プラン（`20260727_01a`）も「既存プリセットをベースに差分で新権限を作る」前提で組んでいます。

リリース時にご用意いただく**現行権限と対になるプリセット（master / manager / shop_manager 相当）が、そのままベース候補になる**想定です。ベース選択が無いと、顧客が新しい権限を作るたびに全項目を手で設定することになります。

---

## 依頼後の画面イメージ

```
基本情報

  ベース   [ 店長                  ▼ ]   ※新規作成時のみ表示
  権限名   [ エリアマネージャー       ]
  区分     [ 店舗                  ▼ ]

  [ 基本情報を更新 ]
```

キーは画面に出さず内部で保持（(a) の場合）。

---

## 優先度

| # | 依頼 | 優先度 | 理由 |
|---|---|---|---|
| 4 | ベースのプルダウン | 高 | 無いと顧客が新権限を作れない（実運用が回らない） |
| 3 | キーを非表示・自動生成 | 高 | 顧客の入力事故に直結 |
| 1 | ロール名 → 権限名 | 中 | 呼称統一 |
| 1-2 | 表示ロール → 表示権限 | 中 | 同上。ヘルプページの記述と画面の表記を揃える |
| 1-3 | 会社ロール（権限母体）→ 会社の権限（母体） | 中 | 同上 |
| 2 | 種別 → 区分 | 中 | 呼称統一 |
