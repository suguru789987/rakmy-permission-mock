# 権限設定 ステージング検証 用 設定値（2026-07-27）

検証プラン `20260727_01a_権限設定_検証プラン.tsv` とセットで使用。
本データは**架空・ダミー**（麺屋ミライ）。本番 `rakmy` / `rakmy_server` には接触しない。

---

## 0. この検証で何を確かめるのか

今回の実装は**現行ラクミー経営管理の権限をそのまま移植する**前提。したがって「現行が壊れていないか」は必要条件だが本題ではない。

**本題＝顧客が現行に無い権限を新しく作るとき、意図どおりの権限が作れるか。**

現行は master / manager / shop_manager の3つしかない。顧客が実際に欲しがるのは「経理だけ」「人事だけ」「2店舗だけ見るエリア長」「見るだけの税理士」といった中間の権限で、それをこの権限設定画面で組み立てられるかが価値の全て。

判定は3段階で記録する。

- **◯ 作れる** … 意図どおりの権限がそのまま作れた
- **△ 近似** … 作れるが粒度が足りず、余計に見える／余計に塞がる（どこが過不足かを記録）
- **× 作れない** … その権限は現状の設定項目では表現できない

△ と × こそが成果物。「何が作れないか」が次フェーズ（操作権限・データ範囲・指標粒度）の優先順位を決める。

---

## 1. 環境（IV）

| ID | 変数 | 値 |
|---|---|---|
| IV-001 | ステージングURL | （要記入） |
| IV-002 | 会社 | 麺屋ミライ |
| IV-003 | 店舗1 | 渋谷店 |
| IV-004 | 店舗2 | 新宿店 |
| IV-005 | 店舗3 | 池袋店 |

店舗は3つ必要（エリア長シナリオで「見せる2店舗／見せない1店舗」を作るため）。

---

## 2. 基本情報の項目（実UI ↔ 仕様）

ステージングの「ロール編集 → 基本情報」は仕様と項目が異なる。変更依頼は `20260727_02_権限設定_UI変更依頼.md`。
**検証は現状のUIで実施し**、変更後に該当ケース（K-001〜003）だけ再確認する。

| 実UI | 仕様（依頼後） | 検証時の入力 |
|---|---|---|
| キー | 画面に出さない（内部で自動生成） | `kensho_keiri` など シナリオごとに一意の英数字 |
| ロール名 | 権限名 | `検証_経理担当` など |
| 種別 | 区分 | 店舗／会社 |
| （無い） | ベース（権限テンプレート） | プルダウンがあれば選ぶ。無ければ全項目を手設定 |

ベース選択が無い場合、各シナリオの「ベース＝◯◯相当」は**全項目を手で設定する**手順に読み替える。

---

## 3. 設定項目の対応表（モック仕様 ↔ ステージング）

権限編集の設定単位は画面カテゴリ。今回のステージングは**ページ単位の閲覧制限**なので、各カテゴリが `閲覧` / `なし` の2値で設定できる想定。

検証時、ステージングの設定項目名がモックと違う場合は**実際の項目名を備考に記録**する（仕様とのズレ自体が所見）。

| モックのカテゴリ | 区分 | 画面 |
|---|---|---|
| ダッシュボード | 共通 | ランディング（仕様では閲覧固定＝設定項目なし） |
| 売上分析 | 共通 | 売上分析 |
| 仕入分析 | 共通 | 仕入分析 |
| 詳細分析 | 共通 | 詳細分析 |
| 集計 | 共通 | 集計・予実 |
| 従業員（閲覧） | 共通 | 従業員一覧の閲覧 |
| 締め状況（閲覧） | 共通 | 締め状況の閲覧 |
| 従業員管理 | 共通 | 従業員の登録・編集（給与含む） |
| 締め管理 | 共通 | 締め処理 |
| 費用設定 | 店舗 | 費用設定 |
| 予算設定 | 店舗 | 予算設定 |
| 店舗情報 | 店舗 | 店舗情報 |
| 会社情報 | 会社 | 会社情報 |
| お支払い | 会社 | お支払い |
| ユーザー | 共通 | 管理者・権限設定 |

---

## 4. 検証シナリオ（顧客が作りたい「今ない権限」）

各シナリオは **ベースとなるプリセット → 変更する設定項目 → できること／できないこと** の形で書く。
ゼロから作らず既存プリセットからの差分で作るのは、実際の顧客運用がその形になるため。

---

### SC-01 経理担当 ｜ 全社のお金は見る、人の情報は見せない

現行に無い権限の代表例。manager（本部管理）から人事系を落として作る。

**作り方**

1. 権限設定 → ＋権限を新規作成
2. ベース＝ **manager 相当プリセット**
3. 権限名＝ `検証_経理担当` ／ 区分＝ **会社**
4. 以下だけ変更する

| 設定項目 | 設定値 |
|---|---|
| 売上分析 | 閲覧 |
| 仕入分析 | 閲覧 |
| 詳細分析 | 閲覧 |
| 集計 | 閲覧 |
| お支払い | 閲覧 |
| 費用設定 | 閲覧 |
| 予算設定 | 閲覧 |
| **従業員管理** | **なし** |
| **従業員（閲覧）** | **なし** |
| ユーザー | なし |

**できること（期待）**
- 売上分析・仕入分析・詳細分析・集計が開ける
- お支払い・費用設定・予算設定が開ける
- 全社（3店舗合算）の数値が見える

**できないこと（期待）**
- 従業員管理・従業員（閲覧）がメニューに出ない。直URLでも入れない
- 権限設定（ユーザー）が開けない

**争点＝ここを見る**
経理には「人件費の総額は見せたいが、個人の給与は見せたくない」というニーズがある。今回は従業員管理をページ丸ごと塞ぐしかないため、**人件費の集計を見せようとすると個人給与まで開いてしまう／個人給与を塞ぐと人件費集計も塞がる**という過不足が起きないか。起きるなら △（給与の分離が次フェーズ必要）。

---

### SC-02 人事担当 ｜ 人は見る、売上・利益は見せない

**作り方**

1. ベース＝ **manager 相当プリセット** ／ 権限名＝ `検証_人事担当` ／ 区分＝ **会社**
2. 変更点

| 設定項目 | 設定値 |
|---|---|
| 従業員（閲覧） | 閲覧 |
| 従業員管理 | 閲覧 |
| 締め状況（閲覧） | 閲覧 |
| 締め管理 | 閲覧 |
| **売上分析** | **なし** |
| **仕入分析** | **なし** |
| **詳細分析** | **なし** |
| **集計** | **なし** |
| **お支払い** | **なし** |

**できること（期待）**
- 従業員・締め関連の画面が開ける

**できないこと（期待）**
- 売上分析・仕入分析・詳細分析・集計・お支払いが開けない

**争点＝ここを見る（最重要）**
ダッシュボードは仕様上「閲覧固定（全ロール常時表示）」で、売上などの数値が並ぶ。売上分析をOFFにしても、**ダッシュボード経由で売上が見えてしまわないか**。見えるならページ単位の制限では「売上を見せない権限」は作れず、× または △（指標単位の制御が必要）。

---

### SC-03 エリアマネージャー ｜ 担当2店舗だけ見る

**作り方**

1. ベース＝ **shop_manager（店長）相当プリセット** ／ 権限名＝ `検証_エリア長` ／ 区分＝ **店舗**
2. 権限内容は店長のまま変更しない
3. ユーザー割当で **渋谷店・新宿店の2店舗**を紐付ける（池袋店は付けない）

**できること（期待）**
- 渋谷店・新宿店の売上・仕入・従業員が見える
- 2店舗を切り替えられる

**できないこと（期待）**
- **池袋店のデータが一切見えない**（店舗切替に出ない／集計にも含まれない）

**争点＝ここを見る（最重要）**
今回の実装は**ページ単位の閲覧制限**であって、データ範囲（どの店舗の行を返すか）の制御ではない。区分＝店舗にしても、担当外の池袋店のデータが混ざるなら「エリア長」という権限は作れない（×）。この場合、顧客が最も欲しがる中間権限が作れないことになるため、次フェーズの最優先項目として記録する。

複数店舗の紐付けそのものができない場合も同じく × として記録する。

---

### SC-04 アルバイトリーダー ｜ 自店舗の売上だけ見る、原価と人件費は見せない

**作り方**

1. ベース＝ **shop_manager（店長）相当プリセット** ／ 権限名＝ `検証_バイトリーダー` ／ 区分＝ **店舗**
2. 変更点

| 設定項目 | 設定値 |
|---|---|
| 売上分析 | 閲覧 |
| **仕入分析** | **なし** |
| **詳細分析** | **なし** |
| **従業員（閲覧）** | **なし** |
| **従業員管理** | **なし** |
| **費用設定** | **なし** |
| **予算設定** | **なし** |
| **締め管理** | **なし** |

3. 割当は渋谷店のみ

**できること（期待）**
- 売上分析だけが開ける

**できないこと（期待）**
- 仕入分析・詳細分析・従業員・費用・予算・締めが開けない

**争点＝ここを見る**
売上分析の画面内に**原価率・粗利・人件費率**が含まれていないか。含まれていれば、仕入分析をOFFにしても原価が見えてしまい「売上だけ見せる」権限は作れない（△＝指標単位の制御が必要）。

---

### SC-05 顧問税理士・監査役 ｜ 全社を見るだけ、一切変更させない

**作り方**

1. ベース＝ **manager 相当プリセット** ／ 権限名＝ `検証_閲覧のみ` ／ 区分＝ **会社**
2. 全ページを **閲覧** にする（なしにはしない）

**できること（期待）**
- 全画面が開ける

**できないこと（期待）**
- **どの画面でも保存・登録・編集・削除ができない**

**争点＝ここを見る（最重要）**
今回の実装は閲覧制限のみで、操作（登録・編集・削除）の制御が入っていない可能性が高い。その場合、**閲覧を許可した設定画面で保存ボタンが押せて実際に更新できてしまう**。そうなると「見るだけの権限」は作れず ×。顧客ニーズとして頻出（税理士・親会社・監査）なので、操作権限の実装優先度を決める材料になる。

具体的には 費用設定 を開いて金額を変更 → 保存 を試し、**実際に保存されるかどうか**を確認する（ボタンが押せるだけでなく、値が更新されるか）。

---

### SC-06 店舗スタッフ ｜ 打刻と自分の情報だけ

現行の shop_manager より下の権限。最小構成で締め出されないかを見る。

**作り方**

1. ベース＝ **shop_manager（店長）相当プリセット** ／ 権限名＝ `検証_スタッフ` ／ 区分＝ **店舗**
2. ダッシュボード以外の全項目を **なし** にする
3. 割当は渋谷店のみ

**できること（期待）**
- ログインでき、ダッシュボードに着地する

**できないこと（期待）**
- 他のすべての画面が開けない

**争点＝ここを見る**
全項目OFFのロールがログインしたとき、**空白画面・エラー・リダイレクトループにならないか**。ダッシュボードが閲覧固定なら着地できるはず。ここが壊れると「権限を絞ると使えなくなる」ため、絞る運用そのものができない。

---

## 5. 検証ユーザー

メールは `s.tsutsui+xxx@rakmy.jp` のプラスアドレッシングで受信を集約。

| ID | 氏名 | メール | 割当ロール | 対象店舗 |
|---|---|---|---|---|
| US-01 | 検証 経理花子 | s.tsutsui+pm01@rakmy.jp | 検証_経理担当 | （全社） |
| US-02 | 検証 人事次郎 | s.tsutsui+pm02@rakmy.jp | 検証_人事担当 | （全社） |
| US-03 | 検証 エリア三郎 | s.tsutsui+pm03@rakmy.jp | 検証_エリア長 | 渋谷店・新宿店 |
| US-04 | 検証 バイト四郎 | s.tsutsui+pm04@rakmy.jp | 検証_バイトリーダー | 渋谷店 |
| US-05 | 検証 税理士五郎 | s.tsutsui+pm05@rakmy.jp | 検証_閲覧のみ | （全社） |
| US-06 | 検証 スタッフ六子 | s.tsutsui+pm06@rakmy.jp | 検証_スタッフ | 渋谷店 |
| US-07 | 検証 付替七海 | s.tsutsui+pm07@rakmy.jp | 検証_エリア長 → 検証_経理担当 に付け替え | 新宿店 |

**1人1ロール（2026-08-03 決定）** のため、US-07 は兼務ではなく**権限の付け替え**を検証する。最初に検証_エリア長を割り当て、その後に検証_経理担当へ付け替える。

1. 割当編集はラジオで、同時に2つは選べない
2. 付け替え前に「選ぶと『検証_エリア長』から置き換わります」が行に表示される
3. 保存後は経理担当の画面だけが開き、エリア長固有の画面は開けない

**合算されていれば両方の画面が開いてしまう＝NG。**

---

## 6. 現行権限の移植確認（必要条件・最小限）

本題ではないが、壊れていたらリリースできないので3件だけ確認する。

| ID | 現行権限 | 確認方法 |
|---|---|---|
| PS-01 | master | プリセット適用**前**に全メニューをスクショ → 適用後に同一アカウントで再取得し完全一致 |
| PS-02 | manager | 同上 |
| PS-03 | shop_manager | 同上 |

適用前のスクショは**リリース前にしか取れない**ので最初に着手する。

---

## 7. 検証前にエンジニアへ確認する点

1. **プリセットは検証時点で入っているか**。入っていない場合、シナリオのベース選択が使えないため、ゼロから作る手順に読み替える必要がある。
2. **「会社スコープ／店舗スコープ」はどちらの軸か**。画面アクセスの区分だけか、データ範囲（他店の行を返さない）まで含むか。SC-03 の判定基準が変わる。
3. **操作（登録・編集・削除）の制御は入っているか**。未実装なら SC-05 は不合格ではなく「次フェーズの宿題」として扱う。
4. **ダッシュボードの扱い**。閲覧固定か、権限で塞げるか。塞げない場合、そこに出る指標は誰に見えるのか。
5. **権限が未定義の新規ページの既定値**。「なし」で始まるか（fail-closed）、素通しか（fail-open）。
