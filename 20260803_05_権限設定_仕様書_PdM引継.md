# 権限設定機能 仕様書（PdM引き継ぎ版）

2026-08-03 ／ ラクミー経営管理 ／ 対象: `suguru789987/rakmy-permission-mock` build `eedfcd3`（整合監査済み・`20260803_08`）

**動作モック（公開URL・ログイン不要）**
### 👉 https://suguru789987.github.io/rakmy-permission-mock/

---

## 概要

**「誰に何を見せるか」を顧客自身が設定できるようにする機能。** 現行は権限が3種類固定で、顧客側に作る手段が無い。

やることは **① 権限を作る → ② 人に配る → ③ 点検する** の3つだけ。

```
① 権限を作る            ② 人に配る                  ③ 点検する
┌──────────┐      ┌──────────┐      ┌──────────┐
│  権限一覧      │─────▶│  ユーザー割当   │─────▶│  役割サマリ     │
│              │      │              │      │              │
│ ＋新規作成     │      │ 誰に付けるか    │      │ 誰が何人持つか  │
└──────────┘      └──────────┘      └──────────┘
      │ ①ベースを選ぶ           │ 権限を1つ選ぶ            │
      │ ②権限名・区分           │ 担当店舗を選ぶ           │
      │ ③画面ごとに設定         │                        │
      ▼                       ▼                       ▼
   保存 ────────────────▶ 保存 ────────────────▶ 確認
```

**最短の一本道**

```
権限一覧 → ＋権限を新規作成 → ①ベース → ②権限名・区分 → ③画面ごとに設定
        → 「ユーザー割当で紐付け →」 → 対象者と担当店舗を選ぶ → 保存 → 役割サマリで確認
```

| ステップ | 画面 | やること | 決まること |
|---|---|---|---|
| ① | 権限編集 | 近いテンプレートを選び、画面ごとに 操作／閲覧／なし を設定 | **どの画面を見せるか** |
| ② | ユーザー割当 | 権限を1つ選び、担当店舗を紐付ける | **誰が・どの店のデータを扱えるか** |
| ③ | 役割サマリ | 権限ごとの許可数と割当人数を確認 | 抜け・持たせすぎの検知 |

**押さえるべき3点**

1. **権限はロール経由で配る** — 人に直接付けない。「店長」を1つ作れば店長が10人でも設定は1つ（→ §7-A）
2. **権限と担当店舗は別々に決める** — 権限＝どの画面、担当店舗＝どの店のデータ。同じエリア長でも人ごとに店舗数が違うため（→ §7-B）
3. **1人1ロール** — 付け替えると前の権限は外れる。兼務は専用の権限を作って表現する（→ §3.5・暫定決定）

入口が複数ある画面（権限編集は4入口、割当は3入口）があり、**来た元によって戻り先が変わる**。全26フローは §5.6。

---

## 0. この文書の使い方

後任PdMが**この文書だけで判断・議論・意思決定できる**ことを目的とする。経営向けの要約は `20260803_01`、エンジニア向けの実装設計は `ENFORCEMENT.md` / `docs/P0_FOUNDATION.md`。

まず上の**概要**で全体像を掴んでから、読む順番の推奨：§1 背景 → §3 権限モデル → §5 画面仕様（**§5.6 のケース別遷移フローで実際の動線を掴む**）→ §7 設計判断 → §8 未決事項。**§7 と §8 が引き継ぎの本体**。仕様は読めば分かるが、「なぜそう決めたか」と「何がまだ決まっていないか」は文書がないと失われる。

| 文書 | 役割 | 主な読者 |
|---|---|---|
| 本書 `20260803_05` | 仕様の全体・設計判断・未決事項 | 後任PdM |
| `20260803_06` | 受入条件表（31条件） | 後任PdM・QA |
| `20260803_07` | 検証プラン（**29件**）。操作系と計算系を統合し1本に。**上部の準備（S-01〜S-06）に初期設定を集約**し、設定値は `20260803_10` のデータセットを参照する | 後任PdM・QA |
| `20260803_09` | ケース別遷移フロー（26フロー・全項目） | 後任PdM・QA |
| `20260803_10` | 検証用データセット（金額を1:2:4にして混入店舗を逆算できる設計） | 後任PdM・QA |
| `20260803_01` | 経営向け要約 | 経営 |
| `README.md` | モックの画面仕様（詳細） | PdM・エンジニア |
| `ENFORCEMENT.md` | 本番実装の設計 | エンジニア |
| `ROADMAP.md` | 段階導入の順序 | PdM・エンジニア |
| `PDM_JUDGE.md` / `JUDGMENT_LOG.md` | 過去の判断の時系列記録 | 後任PdM |

---

## 1. 背景と課題

### 1.1 現行の制約

現行ラクミー経営管理の権限は `master` / `manager` / `shop_manager` の**3種類固定**。DBに権限テーブルは無く、アカウント種別で分岐している。顧客側で権限を作る手段は存在しない。

### 1.2 顧客要望の実態

CSに寄せられる要望は「3権限の中間」に集中する。

| 要望 | 現行で応えられるか | 不足しているもの |
|---|---|---|
| 経理担当に全社のお金は見せるが給与は見せない | ✕ | 画面単位の制御 |
| エリアマネージャーに担当2店舗だけ見せる | ✕ | データ範囲の制御 |
| 顧問税理士に閲覧だけさせる | ✕ | 操作（書込）の制御 |
| アルバイトリーダーに売上だけ見せる | ✕ | 画面内の指標単位の制御 |

**4つの要望が要求する制御軸がすべて異なる**点が重要。これが §3 の権限モデルが4軸（画面／機能／指標／データ範囲）になっている理由であり、§4 の段階導入の順序を決めている。

### 1.3 なぜ今やるか

多店舗顧客の受注条件になっている案件がある。また「権限が分けられないので本部の一部しか使っていない」状態は利用率が上がらず、更新時に解約リスクとなる。

---

## 2. スコープと用語

### 2.1 スコープ

**対象** — 会社画面（経営管理 V69）の権限設定機能。権限の定義、ユーザーへの割当、担当店舗の紐付け、指標の可視性設定。

**対象外** — 新しい画面カテゴリ自体の追加（プロダクト側のマスタ機能）。タイムレコ側の権限。従業員台帳の管理。

### 2.2 用語定義

用語の揺れが議論を壊すため、以下で固定する。

| 用語 | 定義 | 誤用されやすい点 |
|---|---|---|
| **権限（ロール）** | 「どの画面をどこまで使えるか」の定義。名前を持つ | 「役職」と混同される |
| **区分** | 権限が会社画面向きか店舗画面向きか（会社／店舗の2値） | 「データ範囲」と混同される |
| **データ範囲（スコープ）** | どの店舗のデータ行を扱えるか（自店舗／指定店舗群／全社） | 「区分」と混同される |
| **割当** | 権限をユーザーに適用する行為 | — |
| **役職** | 従業員台帳の肩書き（店長・スタッフ等）。**権限を決めない参考情報** | 権限だと誤解される |
| **カテゴリ** | 権限設定の単位となる画面のまとまり。27個 | — |
| **機能** | カテゴリ内の個別機能。53個 | — |
| **指標** | 分析画面に表示される数字の項目 | — |

**区分とデータ範囲は独立した別軸**である。「店舗ロールだから自店舗のみ」ではない。区分は画面の出し分け、データ範囲は行の出し分けで、組み合わせは自由。

---

## 3. 権限モデル

### 3.1 4つの制御軸

| 軸 | 制御するもの | 設定場所 | 実装段階 |
|---|---|---|---|
| ① 画面 | どの画面を開けるか | 権限編集 | **今回** |
| ② 機能 | 画面内で何ができるか（なし／閲覧／操作） | 権限編集（画面名クリックで展開） | 次 |
| ③ 指標 | 画面内にどの数字を出すか | 指標一覧／指標条件設定 | 後 |
| ④ データ範囲 | どの店舗の行を返すか | ユーザー割当（担当店舗） | 後 |

### 3.2 権限レベル（3状態）

**`なし ⊂ 閲覧 ⊂ 操作`** の順序権限。**操作＝登録・編集・削除を一体で含む書き込み全般**。

機能の性質により取りうる段階が異なる。

| 種別 | 段階 | 例 |
|---|---|---|
| 常時(base) | 閲覧固定 | ダッシュボード |
| 閲覧専用(view) | なし／閲覧 | 売上分析・集計 |
| 設定(edit) | なし／閲覧／操作 | 納品書・棚卸・従業員管理 |
| 管理(admin) | なし／閲覧／設定可 | 会社情報・お支払い・連携設定 |
| 実行(action) | なし／実行可 | ダウンロード |

**削除を別権限に分けていない。** 「登録できる＝編集も削除もできる」とし、破壊的な削除は実行時の確認ダイアログで担保する。分けると状態が4つになり、設定コストが跳ね上がる割に「登録はできるが削除はできない」の実用ケースが乏しかったため（→ §7-D）。

### 3.3 カテゴリ構成（27カテゴリ53機能）

カテゴリは `ctx` により3種に分かれ、区分によって出し分ける。

| ctx | 意味 | 数 | カテゴリ |
|---|---|---|---|
| `common` | 会社・店舗どちらでも | 17 | ダッシュボード／売上分析／仕入分析／詳細分析／集計／従業員（閲覧）／締め状況（閲覧）／ダッシュボード（カスタマイズ）／ラベル／納品書／売上／棚卸／小口現金／従業員管理／締め管理／ユーザー／店舗間商品移動 |
| `company` | 会社画面のみ | 4 | ダウンロード／店舗（管理）／会社情報／お支払い |
| `shop` | 店舗画面のみ | 6 | 発注先&仕入商品／店舗情報／連携設定／費用設定／休業日設定／予算設定 |

区分＝会社 → 21カテゴリ（common 17 ＋ company 4）／区分＝店舗 → 23カテゴリ（common 17 ＋ shop 6）。

機械可読の辞書は `docs/feature_keys.json`（`category_count: 27` / `feature_count: 53`）。**機能を増減したらこのファイルと `ENFORCEMENT.md` 付録Aを再生成すること**（カウント整合が崩れると検証の網羅判定が壊れる）。

### 3.4 ロール構成

| ロール | 区分 | 位置づけ |
|---|---|---|
| オーナー（全権） | 会社 | 全権固定・編集不可。権限一覧から除外 |
| 会社ロール（経理／人事など） | 会社 | 職務分掌で設定 |
| 店舗ロール（店長／従業員） | 店舗 | 店舗現場。**既定はこの2つ**。エリア長などはここから派生させる |

**エリア長は既定テンプレートに含めない。** 現時点で店長と権限差はなく、顧客の組織によって要否も内容も変わるため、**店長をベースに顧客が作るカスタマイズ例**として扱う。上位の役割という位置づけでもない。差別化が要る場合は権限編集で画面を絞る／広げる、または担当店舗数で行う。

既定テンプレートは **オーナー／本部管理／店長／従業員の4種**（＋ゼロから作るプレーン）。

### 3.5 1人1ロール（2026-08-03 決定・暫定）

1人が同時に持てる権限は**1つ**。付け替えると前の権限は自動的に外れる。

**採用理由** — 「この人は結局何が見られるのか」が権限名だけで確定する。合算方式では権限名を見ても実際の可視範囲が分からず、点検が困難になる。

**副作用** — 兼務（店長 兼 本部経理など）は「店長＋経理」という権限を新設して表現する。兼務の組み合わせだけ権限が増える。

**この判断は暫定である。** 運用して権限が増えすぎるようなら合算方式への回帰を検討する。判断材料は「実際に作られた権限の数」と「そのうち兼務用が何割か」。→ §8-1

---

## 4. 段階導入

権限は**誤って塞げば業務停止、誤って通せば情報漏洩**。どちらも重大なため、blast radius の小さい順に入れる。

| 段階 | 内容 | リスク | 状態 |
|---|---|---|---|
| P0 | データモデル＋保存＋シャドー検証 | 低 | 未着手 |
| **P1** | **画面単位の閲覧制限** | **低〜中** | **ステージング検証中** |
| P2 | 操作（登録・編集・削除）判定 | 中 | 未着手 |
| P3 | データ範囲の強制 | **高** | 未着手 |
| P4 | 指標の可視性 | 中 | 未着手 |
| P5 | 監査ログ・ロックアウト防止・移行 | 中 | 未着手 |

各段階は**シャドー検証 → 部分有効化 → 全面**の3段で進める。詳細は `ROADMAP.md`。

**既存顧客への影響を無くすため、リリース時に現行3権限と同じ動作のプリセットを用意する。** これが移植の要件であり、受入条件 AC-01〜04 に対応する。

---

## 5. 画面仕様

全9画面。各画面の詳細な挙動は `README.md` に記載。ここでは**引き継ぎに必要な要点と、判断が要る箇所**に絞る。

### 5.1 画面一覧と遷移

各画面の入口・出口を含む全経路は **§5.6**。

| ID | 画面 | 役割 | 主な遷移先 |
|---|---|---|---|
| `view-list` | 権限一覧 | 起点。権限×画面のマトリクス | 権限編集／指標一覧／役割サマリ／ユーザー割当 |
| `view-newrole` | 権限編集・新規作成 | 統合ウィザード | 一括割当／元の画面 |
| `view-catalog` | 指標一覧 | 権限ごとの指標ON/OFF | 指標条件設定 |
| `view-condset` | 指標条件設定 | 指標の一括作り込み | マイテンプレート／指標一覧 |
| `view-tpllist` | マイテンプレート | 指標セットの保存・再利用 | 指標条件設定 |
| `view-summary` | 役割サマリ | 権限ごとの俯瞰 | ユーザー割当（絞り込み）／権限編集 |
| `view-users` | ユーザー割当 | 人起点の割当 | 割当編集 |
| `view-assignedit` | 割当編集 | 1人の権限と担当店舗 | 権限編集／新規作成 |
| `view-roleassign` | 一括割当 | 権限起点で複数人へ | 役割サマリ |

**全画面に戻る導線があり、dead-end は無い**（監査済み）。権限編集とユーザー割当の戻るは「来た元＋権限一覧へ」の2ボタン。

### 5.2 権限編集（統合ウィザード）

新規作成と既存編集で**同じ画面を共有**し、タイトルとステップ番号を出し分ける。

| モード | ステップ |
|---|---|
| 新規 | ①ベース → ②基本設定（権限名・区分） → ③権限内容 |
| 編集 | ①基本設定 → ②権限内容（ベース選択は非表示） |

**ステップ順は「識別 → 中身 → 確定」で固定。** 区分を先に決めるのは、区分が③に出る画面リストを絞るため（会社なら店舗専用6画面が出ない）。逆順にすると設定後に区分変更でやり直しになる。

**ベース候補**

| グループ | 候補 |
|---|---|
| ベーステンプレート | 全権オーナー相当／本部管理／店長／従業員（**4種**） |
| 既存権限をコピー | 運用中の各権限（運用デモのみ表示） |
| ゼロから | プレーンテンプレート（全機能なし） |

**ゼロから作らせない設計。** 27画面53機能をゼロ設定するのは非現実的なため、必ず「近いものを選んで差分を直す」形にしている。リリース時の移植プリセットがそのままベース候補になる想定。

**最下部のコミット領域** — 「🔗 ユーザー割当で紐付け →」CTA ＋ 保存／キャンセル。**権限編集はスコープ（担当店舗）を扱わない**（→ §7-B）。例外はガバナンス警告（店舗ロールで店舗未設定の保持者がいる時のみ）。

### 5.3 権限一覧

ロール × カテゴリのマトリクス。**一覧画面権限（青）／設定画面権限（緑）**の2タブ。

- 既定は**折りたたみ**（要約列のみ）。27画面を全部出すと横に長大になり読めないため
- 「👤 表示ロール」で行を絞る／「📌 権限列設定」で列を選ぶ。**列選択は localStorage に記憶**
- **セルクリック → 権限編集の該当カテゴリへズーム＋一時ハイライト**。気づきから修正までを一続きにする
- ヘッダーの会社名プルダウンで**会社／店舗スコープを切替**。行（ロール）と列（画面）が同時に追従する
- オーナーは一覧から除外。専用操作は右上「👑 オーナー操作」に集約

### 5.4 ユーザー割当・割当編集・一括割当

**割当の入口は3つ（意図的に共存）**

| 入口 | 起点 | 用途 |
|---|---|---|
| 一括割当（`view-roleassign`） | 権限 | 新規作成直後・大量配布 |
| ユーザー割当（`view-users`） | 人 | 入社・異動・個別修正 |
| 役割サマリ「割当へ」 | 権限 | 保持者の確認から修正 |

思考の出発点が3通りあるため。ただし**付与の実体は割当画面に集約**しており二重管理は無い。

**割当編集** — 権限は**ラジオで1つ**（先頭に「割当なし」）。別の権限を選ぶと「選ぶと『○○』から置き換わります」を行に表示。店舗ロールを選んだ時のみ店舗ピッカーが出る。

**一括割当の事故防止3点**

| 仕掛け | 防ぐ事故 |
|---|---|
| 区分フィルタ | 本部の経理に店舗ロールを付ける |
| 「現在のロール」列 | 現状を知らずに付け替える |
| 置き換えの明示（⚠ 既に「○○」を保持／✓ この権限で登録済み）＋保存時確認 | 意図しない権限の剥奪 |

**役職（肩書き）は判断材料にしていない。** 「店長」「マネージャー」「店長 兼 本部経理」など命名が顧客ごとにバラバラで信頼できないため、実際の割当ロールを表示して判断させる。

### 5.5 指標一覧・指標条件設定

指標の可否は**各ロールのON/OFFスイッチのみ**。機密ランクは画面に出さず、**初期値の自動決定にのみ内部利用**する。

| 機密度 | 従業員 | 店長・エリア長 | 経理・人事 |
|---|---|---|---|
| 現場OK(open) | ON | ON | ON |
| 店長まで(mgr) | OFF | ON | ON |
| 経営機密(exec) | OFF | OFF | ON |

既定ON実測：従業員＝店舗17／会社0、店長・エリア長＝店舗36／会社8、経理・人事＝店舗36／会社25。

**指標の付与と行スコープは独立。** 会社指標を店舗ロールに付与しても、他店の生データ行は §3.1 ④ により遮断される。付与＝集計値の閲覧許可であって行の解放ではない。

### 5.6 ケース別 遷移フロー（26フロー）

**同じ画面に複数の入口があり、来た元によって戻り先が変わる**のがこの機能の遷移の特徴。「保存したらどこに着地するか」「キャンセルしたらどこに戻るか」を分けて把握する必要がある。

以下は実装（`index.html`）から起こした全経路。各フローの前提条件・関連検証ID・分岐条件を含む完全版は **`20260803_09_権限設定_ケース別遷移フロー.tsv`**（14項目）。

#### 導入

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-01 | 権限テンプレートで一括登録 | 権限テンプレート → 権限一覧 | 権限一覧（登録済みバナー） | —（初回画面） |
| F-02 | 下書きの一時保存 | 権限テンプレート（同一画面に留まる） | 同一画面 | — |
| F-25 | 下書きの破棄 | 権限テンプレート（既定の選択状態に戻る） | 同一画面 | — |

#### 作成

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-03 | 新規作成（一覧起点） | 権限一覧 → 権限編集①ベース → ②基本設定 → ③権限内容 → 保存 → 権限一覧 | 権限一覧 | ← 権限一覧へ |
| F-04 | 新規作成（割当編集起点） | 割当編集 → 権限編集①ベース（**プレーン既定**）→ ②→③ → 保存 → 割当編集 | 割当編集 | ← 割当編集へ ＋ ← 権限一覧へ |
| F-05 | 作成後そのまま割当へ | 権限編集③ → 確認モーダル → 一括割当 → 保存 → 役割サマリ | 役割サマリ（該当ロールをハイライト） | ← 権限一覧へ |

#### 編集（入口4つ・戻り先がすべて異なる）

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-06 | 一覧の行ボタン | 権限一覧 → 権限編集①基本設定 → ②権限内容 → 保存 → 権限一覧 | 権限一覧 | ← 権限一覧へ |
| F-07 | セルクリック | 権限一覧 → 権限編集（**該当カテゴリへズーム＋一時ハイライト**）→ 保存 → 権限一覧 | 権限一覧 | ← 権限一覧へ |
| F-08 | 役割サマリ起点 | 役割サマリ → 権限編集 → 保存 → 役割サマリ（該当ロールをハイライト） | 役割サマリ | ← 役割サマリへ ＋ ← 権限一覧へ |
| F-09 | 割当編集起点 | 割当編集 → 権限編集 → 保存 → 割当編集 | 割当編集 | ← 割当編集へ ＋ ← 権限一覧へ |

**F-09 は §8-2 の論点そのもの。** ここから編集すると、その権限を持つ全員に反映される（1人だけの調整にはならない）。

#### 割当（入口3つ＋操作3種）

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-10 | 一括割当（権限起点・入口1） | 権限編集③ → 一括割当（管理者一覧＋含める＋対象店舗）→ 保存 → 役割サマリ | 役割サマリ | ← 権限一覧へ |
| F-11 | ユーザー割当（人起点・入口2） | 権限一覧 → ユーザー割当（全件）→ 割当を編集 → 割当編集 → 保存 → ユーザー割当 | ユーザー割当 | ← 権限一覧へ |
| F-12 | 役割サマリから（権限起点・入口3） | 役割サマリ → ユーザー割当（**保持者に絞り込み**）→ 割当編集 → 保存 → ユーザー割当 | ユーザー割当（絞り込み維持） | ← 役割サマリへ ＋ ← 権限一覧へ |
| F-13 | 権限の付け替え（1人1ロール） | ユーザー割当 → 割当編集（ラジオ選択）→ 保存 → ユーザー割当 | ユーザー割当 | ← ユーザー割当へ |
| F-14 | 担当店舗の紐付け | 割当編集（**店舗ロール選択時のみ店舗ピッカーが出現**）→ 店舗選択 → 保存 → ユーザー割当 | ユーザー割当 | ← ユーザー割当へ |
| F-15 | 権限を外す（割当なし） | 割当編集 → 保存 → ユーザー割当（未割当と表示） | ユーザー割当 | ← ユーザー割当へ |

#### 指標

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-16 | 指標一覧を開く | 権限一覧 → 指標一覧（店舗/会社タブ）→ 個別ON/OFF → 権限一覧 | **即時反映（保存ボタンなし）** | ← 権限一覧へ |
| F-17 | 指標条件設定で一括作り込み | 指標一覧 → 指標条件設定①土台 → ②編集 →「適用して指標一覧へ」→ 指標一覧 | 指標一覧（適用済み） | キャンセル＝非適用で戻る |
| F-18 | マイテンプレートに保存 | 指標条件設定 → マイテンプレート（保存/読込/更新/削除）→ 指標条件設定 | マイテンプレート一覧 | ← 指標条件設定へ |

**F-16 は保存ボタンが無く即時反映、F-17 は「適用」が必要。** 同じ指標設定でも確定の仕方が違う。

#### オーナー専用

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-19 | 指標一覧（全件・読取） | 権限一覧 → 指標一覧（オーナー・全件・**読取**）→ 権限一覧 | —（読取のみ） | ← 権限一覧へ |
| F-20 | 全権の付与先を指定 | 権限一覧 → 付与先指定モーダル → 権限一覧 | 権限一覧 | モーダルを閉じる |

#### 横断

| ID | ケース | 遷移フロー | 保存時の着地 | 戻る導線 |
|---|---|---|---|---|
| F-21 | 会社／店舗スコープ切替 | 任意の画面 → スコープ選択 → 権限一覧（行×列が追従） | 端末に保持 | — |
| F-22 | 一覧の表示を絞る | 権限一覧（同一画面で再描画） | 即時反映・localStorage に記憶 | — |
| F-23 | 事故防止：既登録の確認 | 一括割当 → 確認モーダル（○○ → △△）→ 保存 → 役割サマリ | 役割サマリ | キャンセル＝保存せず戻る |
| F-24 | 事故防止：自己ロックアウト | 権限編集 → 警告 →（ブロックまたは続行） | — | 権限編集に留まる |
| F-26 | デモモードの切替 | 任意の画面 → 権限一覧（ロール構成が入れ替わる） | 端末に保持 | — |

#### 押さえておくべき分岐

| フロー | 分岐・条件 |
|---|---|
| F-04 | 割当編集から新規作成すると、ベースが「プレーンテンプレート」で開く |
| F-05 | 権限名が未入力なら移動不可。**新規は「キャンセル/保存して移動」の2択、既存は「保存せず移動」も選べる** |
| F-10 | 区分フィルタで対象者を絞る（「すべて表示」で解除）。店舗ロール＆含める＆店舗0はエラー |
| F-12 | 保持者0名なら絞り込まず全件表示＋案内バナー |
| F-13 | 選ぶ前に「選ぶと『○○』から置き換わります」を行に表示。前の権限は自動的に外れる |
| F-14 | 会社ロールは全社のため店舗紐付けなし。店舗0件は保存不可 |
| F-19 | 編集UI（トグル/コピー/条件設定）は非表示。対象ロールを切り替えれば編集可 |
| F-21 | 会社＝会社ロール×21画面、店舗＝店舗ロール×23画面。**列選択はスコープ別に記憶**、表示ロール選択は全ロールに戻る |
| F-23 | 既に別権限を持つ人がいる時のみモーダルが出る |

**全画面に戻る導線があり dead-end は無い**（監査済み）。遷移を変更したら §5.1 の画面一覧と本節、`20260803_09` を同時に更新すること。

**検証との対応** — 26フローすべてに検証ケースが紐付いている（`20260803_09` の関連検証ID列 ⇔ `20260803_07` の対応ID列で双方向）。特に **F-09（割当編集起点の権限編集）は §8-2 の論点そのもの**で、`20260803_07` の **T-07** で「その権限を持つ全員に反映される旨の警告があるか」を実測し判断材料とする。

---

## 6. データモデル（本番実装）

詳細は `ENFORCEMENT.md`。PdMが把握しておくべき骨格のみ。

| テーブル | 役割 |
|---|---|
| `roles` | 権限の定義（名前・区分） |
| `role_permissions` | 権限 × feature_key × level(0/1/2) |
| `user_roles` | ユーザー ↔ 権限 ＋ 担当店舗（`store_ids`） |
| `role_metrics` | 権限 × 指標の可視性 |
| `audit_logs` | 変更履歴 |

**`user_roles.store_ids` が担当店舗の唯一の正解。** 権限定義側には持たせない（→ §7-B）。

**1人1ロールの制約は `user_roles` に一意制約を張るかで表現する。** 暫定決定のため、DB制約で縛るか、アプリ層のみで縛るかは判断が必要（→ §8-1）。DB制約にすると合算方式へ戻す際にマイグレーションが必要になる。**アプリ層のみで縛ることを推奨**。

---

## 7. 設計判断の記録（なぜそう決めたか）

**引き継ぎで最も失われやすい情報。** 各判断には却下した案とその理由を残す。

### A. 権限はロール経由で与える（人に直接付けない）

**却下案** — ユーザーごとに権限を作り込む。

**理由** — 従業員が増えるほど設定が増殖し、「誰がどこまで見えるか」の把握が不可能になる。「店長」を1つ作れば店長が10人でも設定は1つで、修正も一箇所で済む。

**関連** — この原則があるため、§8-2（ユーザー行からの権限編集）が論点になる。

### B. 担当店舗は割当側にのみ持たせる

**却下案** — 権限編集にも「データ範囲」の選択肢を置く（当初はあった。撤去した）。

**理由** — 両方に店舗設定があると「どちらが本当か」が分からず、片方だけ直して事故る。同じ「エリア長」でもAさん2店舗・Bさん3店舗なので、権限側に持たせると担当店舗の組み合わせだけ権限が必要になり破綻する。

**徹底の度合い** — 権限編集③から掌握店舗数・店舗名の**情報表示すら撤去**した。割当由来のデータを権限定義の場に出さない。例外はガバナンス警告のみ。

### C. 区分とデータ範囲は独立した別軸

**却下案** — 「店舗ロール＝自店舗のみ」と固定する。

**理由** — 「従業員だが複数店舗に所属」「店舗ロールだが全社データを見る」といった実運用がある。ロール定義の区分・範囲は**既定値（テンプレ）にすぎず**、実効スコープは割当で束縛する。

### D. 操作＝登録・編集・削除を一体（削除を分けない）

**却下案** — 4状態（なし／閲覧／操作／全て（削除））＋操作詳細トグル。実際に実装したが撤去した。

**理由** — 状態が増えるほど設定コストが跳ね上がる割に、「登録はできるが削除はできない」の実用ケースが乏しかった。破壊的な削除は実行時の確認ダイアログで担保する方が実効的。

### E. 機密ランクを画面に出さない

**却下案** — 指標に🔓🔸🔒バッジを表示する。実装したが撤去した。

**理由** — バッジを出すと顧客が指標を選ぶたびにランクの妥当性を判断させることになる。ランクは初期値の自動決定にのみ使い、画面はシンプルなON/OFFスイッチにする。

### F. 会社指標を店舗ロールに出すことを禁止しない

**却下案** — 店舗ロールには全社集計をハードブロックする。当初そうしていたが撤回した。

**理由** — 「うちは店長にも全社利益を見せる」という顧客がいる。既定OFF＋管理者オプトインとし、ハードブロックはしない。**ただし行データの遮断は独立にハード強制**する。

### G. 「含める」チェックを店舗選択と統合しない

**却下案** — 一括割当で店舗を選んだら自動的に権限を付与する。

**理由** — 誤クリックがそのまま権限付与になる。明示チェックを安全装置として残す。会社ロールは対象店舗が無いため「含める」が唯一の付与手段でもある。

### H'. エリア長を既定テンプレートにしない（2026-08-04）

**却下案** — エリア長を既定の推奨テンプレートに含める（当初はそうしていた）。

**理由** — エリア長は**現時点で店長と権限差がない**。差が出るのは担当店舗数だけで、それは割当で決まる（→ §7-B）。中身が同じテンプレートを2つ並べると、顧客は「どちらを選ぶべきか」を判断できない。さらにエリア長という役割の要否も内容も**顧客の組織構造に依存**するため、標準として提示すると「エリア長はこういうものだ」という誤った既定を押し付けることになる。

**扱い** — **店長をベースに顧客が作るカスタマイズ例**として位置づける。運用デモには作成済みの例として残し「こう作れる」を見せる。既定テンプレートは オーナー／本部管理／店長／従業員 の4種。

### H. 1人1ロール（2026-08-03・暫定）

**却下案** — 複数ロール保持＋合算（当初の設計）。

**理由** — 権限名だけで可視範囲が確定する。合算方式は点検が困難。

**副作用を認識した上での暫定決定。** → §8-1

---

## 8. 未決事項（後任PdMの判断が必要）

### 8-1. 1人1ロールを継続するか【優先度：中・期限＝運用開始3ヶ月後】

**論点** — 兼務のたびに専用権限が増える。増えすぎると権限一覧が読めなくなり、当初の課題に回帰する。

**判断材料** — 顧客ごとの権限総数、うち兼務用の割合。10権限を超え、その3割以上が兼務用なら合算方式への回帰を検討。

**準備** — DB制約で1人1ロールを縛らない（アプリ層のみ）。縛るとマイグレーションが必要になる。

### 8-2. ユーザー行からの権限編集をどう扱うか【優先度：高・期限＝実装前】

**論点** — 「田中さんだけ調整したい」でユーザー行から権限編集に入ると、同じ権限を持つ全員に波及する。事故になる。

**選択肢**

| 案 | 内容 | 副作用 |
|---|---|---|
| A | 権限名の変更を強制（＝その人専用の権限を作る） | 権限が人数分に増殖し §7-A が崩れる |
| B | **警告のみ**（「他に○名が持っています」＋続行／複製を選択） | 無し。複製を選べばAと同じ結果 |
| C | ユーザー行からは編集させない | 遠回りになる |

**推奨は B。** 事故を防ぎつつ権限の増殖も起きない。

### 8-3. 権限一覧をマトリクスから一覧に変えるか【優先度：中・期限＝UI確定前】

**論点** — 現行はマトリクス（権限×27画面）。シンプル一覧（区分・権限名・操作）にする案がある。

**トレードオフ** — マトリクスは権限同士を横並びで比較できるが横に長い。一覧は権限数が増えても崩れないが、可視範囲は別途ボタンで確認することになる。

**併存案** — 既定は一覧、「比較表示」でマトリクス。

**連動** — この判断でタブの階層が決まる。一覧化するなら「権限／ユーザー」の1階層で済む。マトリクス維持なら2階層になる。

### 8-4. 割当対象は全従業員かログイン管理者か【優先度：高・期限＝実装前】

**論点** — 現行は「経営管理にログインする管理者」のみを対象にしている。全従業員（アルバイト含む）を並べる案がある。

**懸念** — 全従業員だとログインしない人まで一覧に出て、誰に権限を付けるべきかが埋もれる。全従業員にするならログイン可否のフィルタが別途必要。

### 8-5. 権限設定をチェックボックス（2値）にするか【優先度：中・期限＝P2着手前】

**論点** — 今回は閲覧制限のみなので2値で足りる。ただし P2（操作制限）で「閲覧はできるが操作はできない」が必要になり、3状態が要る。

**判断** — 今2値にして P2 で作り直すか、最初から3状態にするか。

### 8-6. 検証で確定させる3点【優先度：高・期限＝検証完了時】

以下は検証（`20260803_07`）の実測で確定する。結果次第で P2/P3 の前倒しを判断する。

| 確認事項 | 該当なら何が作れないか |
|---|---|
| 担当外店舗のデータが混ざるか | エリアマネージャー |
| 閲覧のみの権限で保存できてしまうか | 見るだけの権限（税理士・親会社） |
| ダッシュボード経由で売上が漏れるか | 売上を見せない権限（人事） |

---

## 9. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| 誤って塞ぐ | 業務停止 | シャドー検証で過剰ブロックを事前検出。fail-open から段階切替 |
| 誤って通す | 情報漏洩 | 未定義は fail-closed（既定なし）。直URLもブロック |
| 自己ロックアウト | 運用停止 | 最後の全権は外せない。自ロールから権限設定を外す操作に警告 |
| 移行漏れ | 既存顧客が使えなくなる | 現行3権限と同じ動作のプリセット。移行前後の判定一致100%を受入条件に |
| 権限の増殖 | 管理不能 | 1人1ロールの副作用。§8-1 で見直し |
| 仕様とモックの乖離 | 議論が空転 | 定期監査。機能増減時は `feature_keys.json` を再生成 |

---

## 10. 引き継ぎメモ

### 10.1 直近の作業状態

- モックは公開URLで動作。build `c48a953`
- ステージングに P1（画面単位の閲覧制限）がデプロイ済み。**検証は未実施**
- エンジニアへのUI変更依頼（`20260727_02`）は**未送付**。ロール名→権限名、種別→区分、キーの非表示、ベーステンプレートのプルダウン追加の4件
- 設計変更（`20260803_04`）のうち1人1ロールのみ反映済み。残り2点は §8-2 / §8-3

### 10.2 運用上の注意

- **Pages のビルド方式は「GitHub Actions」必須**。legacy に戻すと SHA 注入が無効化され、全員が古いキャッシュを見続ける不具合が再発する
- 「反映されない」はキャッシュで片付けず、**ライブHTMLを curl して `__BUILD__` を確認**する
- モックのロジック変更は Node の vm ＋DOMスタブで実行検証する（`HANDOFF.md` §検証手段）
- **本番 `rakmy` / `rakmy_server` には接触しない。** 実テーブル名・列名は public モックに出さない。顧客PIIはダミー（麺屋ミライ）

### 10.3 未送付・未着手の一覧

| 項目 | 状態 |
|---|---|
| UI変更依頼のエンジニア送付 | 未 |
| ステージング検証の実施 | 未 |
| §8-2 / §8-3 / §8-4 の判断 | 未 |
| P0（データモデル）の着手 | 未 |
